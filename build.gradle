plugins {
    id 'java'
}

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.seleniumhq.selenium:selenium-java:4.36.0'
    implementation 'io.github.bonigarcia:webdrivermanager:6.1.0'
    testImplementation 'org.testng:testng:7.9.0'
}

test {
    useTestNG()

    // путь к тестовым классам
    testClassesDirs = files('test/java')

    // путь к тестовым ресурсам (если есть)
    classpath += files('test/resources')
}